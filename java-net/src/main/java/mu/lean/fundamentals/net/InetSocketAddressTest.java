package mu.lean.fundamentals.net;

import java.io.IOException;

public class InetSocketAddressTest {
	
	// This class implements an IP Socket Address (IP address + port number) 
	
	// It can also be a pair (hostname + port number), in which case an attempt 
	// will be made to resolve the hostname. 
	
	// If resolution fails then the address is said to be unresolved but can still be used on 
	// some circumstances like connecting through a proxy. 
	public static void main(String[] args) throws IOException {
//		InetSocketAddress(InetAddress addr, int port) 
//		InetSocketAddress(int port) 
//		InetSocketAddress(String hostname, int port) 
		
	}

}
